import { render } from '@testing-library/react';

import RootWrapper from './RootWrapper';

describe('RootWrapper', () => {
  it('renders the RootWrapper component', () => {
    render(<RootWrapper />);
  });
});
